Loko Immeuble — backend (Spring Boot)

Quick start

Requirements: Java 17+, Maven

Run locally:

```bash
cd backend
mvn spring-boot:run
```

API
- GET /api/properties — list sample properties (JSON)

This is a minimal starter backend to be extended with persistence, authentication, and business logic.
